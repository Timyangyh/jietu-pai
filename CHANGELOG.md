# Changelog

## v0.1.3 - 2026-07-01

- UI 自动化验证完成后，自动隐藏并删除本次新增的 Mock 测试任务目录，避免测试占位图堆在相册和本地任务目录里。
- 相册标题新增一键清空历史按钮，可一次隐藏当前相册里的所有历史任务。
- 本地服务新增批量清理相册历史接口，用于插件和自动化验证清理任务。

## v0.1.2 - 2026-07-01

- 修复生图任务卡在“生成中”的问题：本地服务现在会后台执行任务，插件按任务状态轮询结果。
- 为 OpenAI、OpenRouter、Gemini 和自定义兼容 API 的识图、生图、图片下载请求增加超时保护。
- 生图失败、请求中断、服务重启或第三方 API 长时间无响应时，任务会写入 `failed` 和错误原因，不再永久停留在 `running`。
- 旧的卡住任务在刷新后会自动转成失败状态，并提示可重试或切换生图 API。
- 识图 API 和生图 API 可分开选择；未配置的 API 也可以先选择再配置。
- 打包脚本增加私人数据检查，插件包不会包含 `.env`、`runs/`、本地任务历史、Provider 设置、相册记录、token、key 或其他本地历史文件。

## v0.1.1 - 2026-07-01

- 修复第三方 Provider 配置和选择逻辑。
- 支持 OpenRouter、Gemini Nano Banana 和自定义兼容 API 的本地配置。
