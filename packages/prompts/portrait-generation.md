# Generation Job

请基于 `reference.png` 的摄影风格，将 `subject-*.png` 中的人物生成成同风格写真。

要求：
1. 保持人物图的主要面部特征、发型方向和人物气质。
2. 参考图只提供光线、构图、色彩、服装氛围和场景关系。
3. 不复制参考图中的具体人物身份。
4. 输出指定数量的 `4:5` 竖图，命名为 `image-01.png`、`image-02.png`。
5. 不生成露骨、冒充、侵权或带水印文字的内容。
